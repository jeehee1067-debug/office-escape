# Firebase 보안 규칙 (`database.rules.json`)

Firebase 콘솔 → **Realtime Database → 규칙** 탭에 `database.rules.json` 내용을 그대로 붙여넣고 **게시**하세요.
규칙 파일은 순수 JSON 이어야 해서(주석·설명 키를 넣으면 콘솔이 거부합니다) 설명은 이 문서에 둡니다.

## 무엇을 막는가

| 데이터 | 규칙 |
|---|---|
| 점수 기록 `runs/{uid}/rooms/{방}/solves/{단서}` | **한 번 쓰면 덮어쓸 수 없음.** 배점은 그 문제의 배점표(`bank/scores/{문제id}`, 관리자 로그인 때 올라감)와 같은 값만, 기록 시각은 서버 시각만 허용 |
| 방 종료 기록 `…/done` | 한 번만. 시간 0~300초, 보너스 0~3000, 시간 초과면 보너스 0 |
| 오답·함정 카운트 | 늘어나기만 함 (줄이거나 지울 수 없음) |
| 게임 진행 `global` / 팀 `teams` / 배점표 `bank` | **관리자 키를 아는 사람만** 쓸 수 있음. 참가자는 `global/state`, `teams/state` 만 읽음 — 키는 읽을 수 없는 자리에 있음 |
| 관리자 키 `config/adminKey` | 최초 1회만 기록, 아무도 읽을 수 없음 (비밀번호의 SHA-256) |
| 채팅 | 한 번 보내면 수정·삭제 불가, 100자 제한, 서버 시각만 |
| 전체 초기화 / 방 리셋 | 관리자가 `global/state/wipeAt`(또는 `roomReset`)을 찍은 뒤 **15초 안에만** 삭제가 허용됨 |

## 알려진 한계 — 익명 로그인 없이 쓰는 경우

이 게임은 Firebase 인증 없이 기기별 임의 ID 로 동작합니다. 그래서 규칙은
**"기록을 고칠 수 없다"** 는 보장하지만, **"누가 썼는지"** 는 보장하지 못합니다.
개발자 도구를 열 줄 아는 참가자라면 다른 사람의 아직 비어 있는 단서 칸에 먼저 기록을 써 넣는 식의 방해가 이론상 가능합니다.

이것까지 막으려면 Firebase 콘솔에서 **Authentication → 익명 로그인** 을 켜고
규칙의 `$uid` 자리에 `auth.uid === $uid` 조건을 더하면 됩니다. (사내 행사 규모에서는 대개 필요하지 않습니다.)
